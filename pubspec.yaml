name: amiraly
description: "A new Flutter project."
# The following line prevents the package from being accidentally published to
# pub.dev using `flutter pub publish`. This is preferred for private packages.
publish_to: 'none' # Remove this line if you wish to publish to pub.dev

# The following defines the version and build number for your application.
# A version number is three numbers separated by dots, like 1.2.43
# followed by an optional build number separated by a +.
# Both the version and the builder number may be overridden in flutter
# build by specifying --build-name and --build-number, respectively.
# In Android, build-name is used as versionName while build-number used as versionCode.
# Read more about Android versioning at https://developer.android.com/studio/publish/versioning
# In iOS, build-name is used as CFBundleShortVersionString while build-number is used as CFBundleVersion.
# Read more about iOS versioning at
# https://developer.apple.com/library/archive/documentation/General/Reference/InfoPlistKeyReference/Articles/CoreFoundationKeys.html
# In Windows, build-name is used as the major, minor, and patch parts
# of the product and file versions while build-number is used as the build suffix.
version: 1.0.0+1

environment:
  sdk: ^3.6.0

# Dependencies specify other packages that your package needs in order to work.
# To automatically upgrade your package dependencies to the latest versions
# consider running `flutter pub upgrade --major-versions`. Alternatively,
# dependencies can be manually updated by changing the version numbers below to
# the latest version available on pub.dev. To see which dependencies have newer
# versions available, run `flutter pub outdated`.
dependencies:
  flutter:
    sdk: flutter

  # The following adds the Cupertino Icons font to your application.
  # Use with the CupertinoIcons class for iOS style icons.


  http: ^1.5.0
  firebase_core: ^3.15.2
  provider: ^6.1.2
  get: ^4.7.2
  shared_preferences: ^2.5.3
  intl: ^0.20.2
  device_preview: ^1.1.0
  cached_network_image: ^3.4.1
  pdf: ^3.10.8
  printing: ^5.11.1
  share_plus: ^10.1.4
  path: ^1.9.0
  iconsax: ^0.0.8
  curved_labeled_navigation_bar: ^2.0.6
  carousel_slider: ^5.1.1
  smooth_page_indicator: ^1.2.1
  syncfusion_flutter_gauges: ^29.1.38
  animate_do: ^4.2.0
  shimmer: ^3.0.0
  supabase_flutter: ^2.9.1
  flutter_dotenv: ^5.1.0
  fl_chart: ^0.71.0
  flutter_inappwebview: ^6.1.5
  flutter_spinkit: ^5.2.2
  liquid_pull_to_refresh: ^3.0.1
  flutter_screenutil: ^5.9.3
  photo_view: ^0.15.0
  firebase_messaging: ^15.2.10
  flutter_local_notifications: ^19.4.2
  googleapis_auth: ^2.0.0
  firebase_analytics: ^11.6.0
  cloud_functions: ^5.6.2
  image: ^4.5.4
  dotenv: ^4.2.0
  icons_launcher: ^3.0.3
  go_router: ^16.1.0
  path_provider: ^2.1.5
  dio_cache_interceptor: ^4.0.5
  http_cache_hive_store: ^5.0.1
  hive: ^2.2.3
  dio: ^5.9.0
  encrypt: ^5.0.3
  flutter_secure_storage: ^10.0.0
  infinite_scroll_pagination: ^5.1.1
  connectivity_plus: ^7.0.0
  flutter_riverpod: ^2.6.1
  agora_rtc_engine: ^6.3.2
  permission_handler: ^11.3.1
  audioplayers: ^6.1.1

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0
  flutter_launcher_icons: ^0.14.4

flutter_launcher_icons:
  image_path: "lib/assets/icons/qq4.png"
  adaptive_icon_background: "#000000"
  adaptive_icon_foreground: "lib/assets/icons/qq4.png"
  min_sdk_android: 21
  ios: true
  android: true

# For information on the generic Dart part of this file, see the
# following page: https://dart.dev/tools/pub/pubspec

# The following section is specific to Flutter packages.
flutter:
  # The following line ensures that the Material Icons font is
  # included with your application, so that you can use the icons in
  # the material Icons class.
  uses-material-design: true

  # To add assets to your application, add an assets section, like this:
  assets:
    - .env
    - lib/assets/images/g/ff.jpg
    - lib/assets/images/g/image55.jpg
    - lib/assets/images/g/ministry.jpg
    - lib/assets/images/g/rtop.jpg
    - lib/assets/images/g/qq3.png
    - lib/assets/images/g/qq4.png
    - lib/assets/images/g/qq.png
    - lib/assets/images/g/aaa.jpg
    - lib/assets/images/g/on1.jpg
    - lib/assets/images/g/on2.jpg
    - lib/assets/images/g/on3.jpg
    - lib/assets/images/g/on41.jpg
    - lib/assets/images/g/on42.png
    - lib/assets/sounds/ring.mp3
    - lib/assets/sounds/connect.mp3
    - lib/assets/sounds/hangup.mp3

  # An image asset can refer to one or more resolution-specific "variants", see
  # https://flutter.dev/to/resolution-aware-images

  # For details regarding adding assets from package dependencies, see
  # https://flutter.dev/to/asset-from-package

  # To add custom fonts to your application, add a fonts section here,
  # in this "flutter" section. Each entry in this list should have a
  # "family" key with the font family name, and a "fonts" key with a
  # list giving the asset and other descriptors for the font. For
  # example:
  fonts:

    - family: Changa-Bold
      fonts:
        - asset: lib/assets/fonts/Changa-Bold.ttf
    - family: Changa-Light
      fonts:
        - asset: lib/assets/fonts/Changa-Light.ttf
    - family: BebasNeue_Regular
      fonts:
        - asset: lib/assets/fonts/BebasNeue_Regular.ttf
    - family: digital
      fonts:
        - asset: lib/assets/fonts/Digital.ttf